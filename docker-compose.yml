# docker-compose.yml

services:
  stock-analyzer-app:
    build: .
    container_name: stock_analyzer_ui
    ports:
      - "8501:8501"
    volumes:
      - ./reports:/app/reports
    env_file:
      - .env